# Notebooks

Colab notebooks should be thin orchestration layers.

Rules:

- keep source code in `src/` and `scripts/`;
- use notebooks for setup, execution, and visualization;
- save checkpoints to Google Drive;
- pull code from GitHub at the start of each runtime;
- do not paste secrets into notebook outputs.

